name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  user-mode:
    runs-on: windows-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        preset: [x64-debug, arm64-debug, x86-debug]
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        shell: pwsh
        run: cmake --preset ${{ matrix.preset }}
      - name: Build
        shell: pwsh
        run: cmake --build --preset ${{ matrix.preset }}
      - name: Test native x64
        if: matrix.preset == 'x64-debug'
        shell: pwsh
        run: ctest --preset x64-debug

  driver-compile:
    # Hosted Windows images ship no WDK (stampinf.exe is missing), so this job
    # cannot pass on a hosted runner — build driver packages locally or on a
    # self-hosted runner via workflow_dispatch.
    if: github.event_name == 'workflow_dispatch'
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - name: Build unsigned x64 driver package
        shell: pwsh
        run: .\tools\Build-Driver.ps1 -Architecture x64 -Configuration Debug

  winui-settings:
    runs-on: windows-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        platform: [Win32, x64, ARM64]
    steps:
      - uses: actions/checkout@v4
      - name: Build WinUI 3 settings app
        shell: pwsh
        run: |
          $msbuild = & "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products '*' -find 'MSBuild\**\Bin\MSBuild.exe' | Select-Object -First 1
          & $msbuild .\src\settings\ForwardSlashWindows.Settings.vcxproj /restore /m:1 /p:UseMultiToolTask=false /p:Configuration=Release /p:Platform=${{ matrix.platform }}
          if ($LASTEXITCODE -ne 0) { throw "MSBuild exited with $LASTEXITCODE" }
