[package]
name = "fsw-path"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Pure WSL forward-slash path resolver for Forward Slash Windows."

# INTENTIONALLY EMPTY. fsw-path is the only crate shared across both windows-rs
# version islands, which is only sound while it depends on neither. A PR that
# adds anything here needs to explain why in docs/dependencies.md, and CI
# asserts `cargo tree -p fsw-path` prints exactly one line.
[dependencies]

[lints]
workspace = true
