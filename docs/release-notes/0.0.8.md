- Typing a Linux path in File Explorer, Run or Windows Search now works reliably every time.
- Windows Search opens the folder you typed instead of searching for the path as text.
- Linux paths now work in the address bar in Chrome, Edge, Firefox and Brave.
- Browser support no longer depends on your default search engine or on Windows being in English.
- Enter is no longer silently ignored when a path cannot be opened.
- Paths ending in a dot or a space now open the folder you actually asked for.
- Drive paths such as /mnt/c behave the same way everywhere in the app.
- Open WSL root in the notification area menu works again.
- Automatic updates now download and install correctly.
- The app keeps checking for updates even if your clock was set ahead.
- The update notice no longer keeps coming back after the update is installed.
- Terminal integrations update themselves quietly, without Windows asking permission to change files in your Documents folder.
- When Windows blocks a change, the app now tells you instead of looking like it worked.
- Pausing the app from the notification area now stops it completely.
- Uninstalling cleans up the leftover files and scheduled tasks it used to leave behind.
